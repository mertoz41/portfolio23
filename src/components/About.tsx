import React from "react";
import { Text } from "@chakra-ui/react";
const About = () => {
  return (
    <Text mt={10} fontSize={20}>
      Experienced in creating full stack web and mobile apps using Ruby, Python,
      and JavaScript libraries. Passionate about learning new technologies and
      building minimalist user interfaces, focusing on the essential.
    </Text>
  );
};

export default About;
